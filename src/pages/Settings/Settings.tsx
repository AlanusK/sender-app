import React from "react";
import { SettingsContainer } from "../../containers";

const Settings = () => {
  return (
    <div>
      <SettingsContainer />
    </div>
  );
};

export default Settings;
